export default function taskBlock(trueOrFalse) {
	  const task = false;
	  const task2 = true;

	  if (trueOrFalse) {
		  /* If statement */
		      const task = true;
		      const task2 = false;
		    }

	  return [task, task2];
}
